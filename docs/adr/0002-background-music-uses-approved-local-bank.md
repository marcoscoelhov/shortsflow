# Background music uses an approved local bank

The default source for background music is an approved local bank of licensed tracks, not a generative music API. When the bank is empty, the app can populate it with local synthetic instrumental beds instead of downloading external catalog music; this keeps cron runs unblocked without adding copyright or API quota risk. MiniMax Music remains available only when configured directly or as an explicit API fallback.
